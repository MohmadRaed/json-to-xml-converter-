# json-to-xml-converter-
Here is a version under 350 characters:  JSON to XML Converter Developed a tool that converts JSON data into structured XML format while preserving hierarchy and nested relationships. Implemented parsing logic to handle objects, arrays, and key-value pairs. Demonstrates skills in data processing, file handling, and structured format transformation.
